server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/allandesenv/mercadotech-monorepo.git
          default-label: main
          search-paths: .
  profiles:
    active: native